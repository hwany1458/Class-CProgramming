// 함수
// 반환되는데이터타입 함수명(매개변수) { .. }

// void 대신에 int 썼다 
// == main()함수에서 반환이 있다 라는 의미
// main() 함수의 매개변수가 없는 경우 (=) 함수 입력이 없음

// 교재 256페이지 형식1~5 참조
// main()함수의 반환값이 없을 경우는 void main()으로
int main(int argc, char *argv[])
{
	// ....

    // main()함수의 반환값이 int로 지정될 경우, 적절한 정수값을 return해야
    // 0은 통상적으로 정상적 실행을 의미하도록 값을 반환
	return 0;
}