#include <stdio.h>
void main()
{
    // 제어문자 \n : 줄바꿈 문자 (다음 줄, 즉, 엔터키 입력을 의미) 
    // \t : 탭키 입력을 의미
    // printf()는 어떤 내용을 화면(모니터)에 출력하라는 함수(명령)
    // printf()를 사용하려면, <stdio.h> 헤더파일을 포함해야 함 - 이파일에 정의되어 있음
    printf("안녕 C언어...\n");
    printf("\t이용환\n");
    printf("이현순\n");
    printf("이현서\n");

    // PPT를 보고 아래와 같은 문자열을 화면에 출력하도록 코드를 작성해 보세요.
    //main() function is where program execution begins.
    //All program statements must be terminated by a semicolon.
    //In C, case sensitive.

}