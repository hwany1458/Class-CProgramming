#include <stdio.h>

int main(void) {
    // 1. 히어로의 레벨 (Level)
    int level;

    // 2. 최대 체력 (Health Point)
    int hp;

    // 3. 보유한 골드 (Gold)
    int gold;
    // (아직은 상자만 만들었을 뿐, 안에 내용은 비어있거나 쓰레기 값이 들어있습니다.)

    // 4. 키 (height): 
    double height;

    // 5. 공격 속도 (Attack Speed): 1초에 몇 번 때리는가?
    double attackSpeed;

    // 6. 치명타 확률 (Critical Rate): % 단위
    double criticalRate;

    
    return 0;
}