// 표준입출력(standard input output) 라이브러리 사용을 표시함
#include <stdio.h>

void main()
{
	// 주석 (컴파일에서 빠지는 부분으로 설명 붙일 때)
	// 대소문자를 구별함 (main과 Main을 다른 것으로 인식)
	// main()는 반드시 1개만 필요 : 시작점을 표시

	// printf() : "화면에 출력하세요" 라는 명령
	// 세미콜론(;)은 명령 끝을 의미
	printf("안녕 용환~~");
	// 무엇을 출력하는지를 쌍따옴표("") 사이에 작성
}
